/**
 * Creates an UnexpectedEof error at a given position.
 * @param {number} pos - The position in the input stream at which the error
 *   occured
 */
module.exports = function unexpectedEof(pos) {
  return new Error("Unexpected EOF at position:" + pos)
}
