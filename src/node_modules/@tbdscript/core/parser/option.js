module.exports = function option(parser) {
  return function(input, pos, cok, _, eok) {
    function perr(result) {
      eok(result, pos, input)
    }

    return parser(input, pos, cok, perr, eok, perr)
  }
}
