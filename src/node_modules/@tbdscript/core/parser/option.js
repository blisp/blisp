module.exports = function option(parser) {
  return function(input, pos, cok, cerr, eok, eerr) {
    function pcerr(result) {
      eerr(result, pos, input)
    }

    return parser(input, pos, cok, pcerr, eok, eerr)
  }
}
