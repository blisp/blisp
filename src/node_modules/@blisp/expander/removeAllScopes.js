module.exports = function removeAllScopes(syntax) {
  let result = { ...syntax }
  switch (syntax.type) {
    case "ArrayExpression":
      syntax.elements = syntax.elements.map((element) =>
        removeAllScopes(element)
      )
  }
  delete result.scopes
}
