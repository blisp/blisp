const { memberExpression } = require("@babel/types")
const expand = require("@blisp/expander/expand")

module.exports = function get(form, env) {
  return memberExpression(
    expand(second(form), env),
    expand(third(form), env),
    true
  )
}
