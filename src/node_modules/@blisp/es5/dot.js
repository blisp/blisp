const { memberExpression } = require("@babel/types")
const expand = require("@blisp/expander/expand")

module.exports = function dot(form, env) {
  return memberExpression(expand(second(form), env), expand(third(form), env))
}
