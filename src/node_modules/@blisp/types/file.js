const compile = require("@blisp/compiler/compile")
const { file } = require("@babel/types")

// (#%program ((statment) stmt1 ))
function File(form) {
  return file(compile.call(this, form[1]))
}

module.exports = { [Symbol.for("#%file")]: File }
