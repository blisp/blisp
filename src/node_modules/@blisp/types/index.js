module.exports = [require("./member-expression")].reduce(
  (env, { identifiers, macro }) => {
    for (const identifier of identifiers) {
      env[identifier] = macro
    }
    return env
  },
  {}
)
