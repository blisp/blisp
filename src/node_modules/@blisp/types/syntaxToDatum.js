module.exports = function syntaxToDatum(syntax) {
  switch (syntax.type) {
    case "ArrayExpression":
      return syntax.elements
    default:
      // Assume a literal node
      return syntax.value
  }
}
