import { isModuleDeclaration } from "@babel/types"

function ObjectProperty(form, env) {
  return {
    type: "ObjectProperty",
    key: form.arguments[0],
    value: expand(form.arguments[1], env),
  }
}

ObjectProperty.type = "ObjectProperty"
ObjectProperty.helpers = {
  nth(form, n) {
    switch (n) {
      case 0:
        return "init"
      case 1:
        return form.key
      case 2:
        return form.value
    }
  },
}

module.exports = {
  init: ObjectProperty,
}
