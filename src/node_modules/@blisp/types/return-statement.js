const compile = require("@blisp/compiler/compile")
const { returnStatement } = require("@babel/types")

function ReturnStatement(form) {
  return returnStatement(
    form[1] != null ? compile.call(this, form[1]) : undefined
  )
}

module.exports = {
  [Symbol.for("#%return")]: ReturnStatement,
}
